// Route-related types and interfaces below

// Define a type for route options, specifying the structure of objects passed to routes.
export type RouteOptions = {
    prismaClient: unknown;
    redisClient: unknown;
}